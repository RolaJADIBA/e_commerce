{% extends 'base.html.twig' %}

{% block body %}

    <div class="container content">
        <div class="row justify-content-center style fade-in">
            <div class="col-12 slide">
                <img src="{{ asset('logo.png') }}" alt="logo" height="300">
            </div>
            <div class="col-12">
                <div class='barreleft slideleft'></div>
            </div>

            <div class="col-12 ">
                <div class="fond"></div>
                <a href="{{path('accueil')}}" class="btn btnAccueil text-center">Decouvrez nos produits</a>
            </div>

        </div>
    </div>

{% endblock %}
