{% extends 'base.html.twig' %}

{% block title %}Edit Option Groupe{% endblock %}

{% block body %}
    {# <h1>Edit OptionGroupe</h1>

    {{ include('option_groupe/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('option_groupe_index') }}">back to list</a>

    {{ include('option_groupe/_delete_form.html.twig') }} #}


    <section class="container">
    
        {% for message in app.flashes('success') %}
            <div class='alert alert-success'>
                {{message}}
            </div>
        {% endfor %}

        <h1>Modifier le Option Groupe</h1>
            
            {{ form_start(form) }}
        
        <div class="row">
            
            <div class="col-md-8 p-3">
                {{ form_row(form.nom, { 'attr': { 'class':'form-control'} }) }}
                {{ form_row(form.categorie, { 'attr': { 'class':'form-control'} }) }}

            </div>

            <div class="col-md-4 p-3 border-0 ">
                <button class="btn btn-primary btn-block btn-sm mt-3">Modifier</button>
                <a href="{{ path('option_groupe_index') }}" class="btn btn-outline-secondary btn-block btn-sm mt-3">Retour à la liste des options groupe </a>
                <a href="{{ path('option_groupe_delete', {'id': option_groupe.id}) }}" class="btn btn-danger btn-block btn-sm mt-3">Supprimer</a>
            </div>
            </div>
            {{ form_end(form) }}

    </section>

{% endblock %}
