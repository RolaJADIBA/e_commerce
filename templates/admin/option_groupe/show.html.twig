{% extends 'base.html.twig' %}

{% block title %}OptionGroupe{% endblock %}

{% block body %}
    {# <h1>OptionGroupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ option_groupe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('option_groupe_index') }}">back to list</a>

    <a href="{{ path('option_groupe_edit', {'id': option_groupe.id}) }}">edit</a>

    {{ include('option_groupe/_delete_form.html.twig') }} #}



<div class="container">

    <h5 class="mt-5">Option :{{ option_groupe.id }}</h5>

    <table class="table border mt-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ option_groupe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info btn-sm" href="{{ path('option_groupe_index') }}">Retour à la liste des option groupe</a>

    <a class="btn btn-primary btn-sm" href="{{ path('option_groupe_edit', {'id': option_groupe.id}) }}">Modifier</a>

    <a class="btn btn-danger btn-sm" href="{{ path('option_groupe_delete', {'id': option_groupe.id}) }}">Supprimer</a>

</div>

{% endblock %}
